---
generate: /docs/*
wrapper: wrappers/main
docsDir: md
---

<%# Show page %>
<div class="grid grid-cols-5 w-full">
  <div class="col-span-5 bg-blue-600 rounded-lg text-white px-4 py-1 mb-4 font-black">
    AIRFRY
  </div>
  <div class="col-span-1 prose-sm prose-blue my-2">
    <% pages.forEach(item=>{ %>
    <div class="mr-2 my-0 flex flex-row">
      <% if (pagePath == "/docs/"+item.url ) { %>
      <div class="font-bold"><%= item.title %></div>
      <% } else { %>
      <a href="/docs/<%= item.url %>/"><%= item.title %></a>
      <% } %>
    </div>
    <% }); %>
  </div>
  <div class="col-span-3 prose prose-red m-auto py-8">
    <% if (!Boolean(html)) { %>
    <span class="font-black">TODO</span>
    <% } else { %>
    <%- html %>
    <% } %>
  </div>
  <% if (locals.upNextTitle) { %>
  <div class="col-span-5 bg-blue-600 rounded-lg text-white px-4 py-1 mb-4 font-black">
    Up Next: <%= upNextTitle %>
  </div>
  <% } %>
</div>

<script entry>
  // this is a test
</script>

<script generate-use:"generators/docsMD">
</script>